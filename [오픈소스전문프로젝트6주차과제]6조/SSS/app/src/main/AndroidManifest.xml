<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.sss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".analyze"></activity>
        <activity android:name=".setting"></activity>
        <activity android:name=".Activity_Alarmsetting"></activity>
        <activity android:name=".Activity_Auto"></activity>
        <activity android:name=".Activity_Datasetting"></activity>
        <activity android:name=".Activity_Manual"></activity>
        <activity android:name=".Activity_Register"></activity>
        <activity android:name=".Activity_RegisterMail"></activity>
        <activity android:name=".Activity_RegisterPhoneNum"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>